services:
  - type: web
    name: snapgram-api
    env: node
    buildCommand: cd server && npm install
    startCommand: node server/src/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
